<div>
    <% if (content.length === 0) { %>
        <p>No Activity found.</p>
    <% } else { %>
        <% content.forEach(function(item) { %>
            <li>
                <a href="./activity/<%= item.title %>">
                    <h1><%= item.title %></h1>
                </a>
                <img src="<%= item.image %>" alt="<%= item.title %>">
                <p><%= item.content %></p>
            </li>
        <% }); %>
    <% } %>
</div>